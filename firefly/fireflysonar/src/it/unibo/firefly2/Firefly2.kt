/* Generated by AN DISI Unibo */ 
package it.unibo.firefly2

import it.unibo.kactor.*
import alice.tuprolog.*
import unibo.basicomm23.*
import unibo.basicomm23.interfaces.*
import unibo.basicomm23.utils.*
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.delay
import kotlinx.coroutines.launch
import kotlinx.coroutines.runBlocking
import it.unibo.kactor.sysUtil.createActor   //Sept2023
//Sept2024
import org.slf4j.Logger
import org.slf4j.LoggerFactory 
import org.json.simple.parser.JSONParser
import org.json.simple.JSONObject


//User imports JAN2024

class Firefly2 ( name: String, scope: CoroutineScope, isconfined: Boolean=false, isdynamic: Boolean=false ) : 
          ActorBasicFsm( name, scope, confined=isconfined, dynamically=isdynamic ){

	override fun getInitialState() : String{
		return "s0"
	}
	override fun getBody() : (ActorBasicFsm.() -> Unit){
		//val interruptedStateTransitions = mutableListOf<Transition>()
		//IF actor.withobj !== null val actor.withobj.name� = actor.withobj.method�ENDIF
		
				var X           = 5
				var Y           = 5
				var Timer       = 3000L
				val NATURAL     = 3000L
				val SYNC_PERIOD = 2000L 
				var inSync      = false
		return { //this:ActionBasciFsm
				state("s0") { //this:State
					action { //it:State
						CommUtils.outblue("firefly2 | periodo naturale=$NATURAL ms")
						//genTimer( actor, state )
					}
					//After Lenzi Aug2002
					sysaction { //it:State
					}	 	 
					 transition( edgeName="goto",targetState="waitToFlash", cond=doswitch() )
				}	 
				state("waitToFlash") { //this:State
					action { //it:State
						//genTimer( actor, state )
					}
					//After Lenzi Aug2002
					sysaction { //it:State
				 	 		stateTimer = TimerActor("timer_waitToFlash", 
				 	 					  scope, context!!, "local_tout_"+name+"_waitToFlash", Timer )  //OCT2023
					}	 	 
					 transition(edgeName="t03",targetState="flash",cond=whenTimeout("local_tout_"+name+"_waitToFlash"))   
					transition(edgeName="t04",targetState="handleObstacleDetected",cond=whenEvent("obstacleDetected"))
					transition(edgeName="t05",targetState="handleObstacleClear",cond=whenEvent("obstacleClear"))
				}	 
				state("flash") { //this:State
					action { //it:State
						forward("cellstate", "cellstate($X,$Y,1)" ,"griddisplay" ) 
						delay(500) 
						forward("cellstate", "cellstate($X,$Y,0)" ,"griddisplay" ) 
						 Timer = if (inSync) SYNC_PERIOD else NATURAL  
						CommUtils.outblue("firefly2 | flash -> period=$Timer ms")
						//genTimer( actor, state )
					}
					//After Lenzi Aug2002
					sysaction { //it:State
					}	 	 
					 transition( edgeName="goto",targetState="waitToFlash", cond=doswitch() )
				}	 
				state("handleObstacleDetected") { //this:State
					action { //it:State
						
									inSync = true
									Timer  = SYNC_PERIOD
						CommUtils.outred("firefly2 | OSTACOLO -> SYNC $SYNC_PERIOD ms")
						//genTimer( actor, state )
					}
					//After Lenzi Aug2002
					sysaction { //it:State
					}	 	 
					 transition( edgeName="goto",targetState="waitToFlash", cond=doswitch() )
				}	 
				state("handleObstacleClear") { //this:State
					action { //it:State
						
									inSync = false
									Timer  = NATURAL
						CommUtils.outblue("firefly2 | CLEAR -> $NATURAL ms")
						//genTimer( actor, state )
					}
					//After Lenzi Aug2002
					sysaction { //it:State
					}	 	 
					 transition( edgeName="goto",targetState="waitToFlash", cond=doswitch() )
				}	 
			}
		}
} 
